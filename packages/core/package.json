{
  "name": "@realpomo/core",
  "version": "1.1.5",
  "private": true,
  "description": "Shared business logic for RealPomo timer",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts"
  }
}
